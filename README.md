# Avtoradio Player - PWA Радиоплеер

## 📋 Описание

Современное прогрессивное веб-приложение (PWA) для прослушивания радиостанции Авторадио с поддержкой HLS потоков и работой на всех устройствах, включая старые версии iOS.

## ✨ Возможности

- 🎵 **Воспроизведение HLS потоков** - поддержка современных браузеров через HLS.js
- 🍎 **Нативная поддержка iOS** - автоматическое переключение на нативный плеер для iPhone/iPad
- 📱 **PWA** - установка на главный экран, работа оффлайн для кэшированных ресурсов
- 🔊 **Управление громкостью** - на Android и Desktop с кнопкой mute
- 🌐 **Адаптивный дизайн** - оптимальный вид на всех экранах
- 🔄 **Автоматическое переподключение** - логика восстановления при сбоях сети
- 🐛 **Подробное логирование** - встроенная система отладки для диагностики проблем
- 🎞️ **Видео плеер** - возможность переключения между радио и видео контентом
- ♿ **Доступность** - поддержка ARIA-атрибутов и клавиатурной навигации

## 📁 Структура проекта

```
avtoradioshow/
├── index.html              # Главная страница приложения
├── App.js                 # Главный класс приложения
├── RadioPlayer.js         # Класс радио плеера
├── VideoPlayer.js         # Класс видео плеера
├── utils.js               # Утилиты и вспомогательные функции
├── global-functions.js    # Глобальные функции (showToast и др.)
├── loging.js              # Система логирования
├── style.css              # Стили интерфейса
├── manifest.json          # PWA манифест
├── sw.js                  # Service Worker
├── hls.js                 # HLS.js библиотека
├── logs.html              # Страница просмотра логов (отладка)
├── favicon.ico            # Иконка браузера
└── assets/
    ├── fonts/all.min.css  # FontAwesome иконки
    └── icon-*.png         # Иконки PWA разных размеров
```

## 🚀 Установка

### 1. Загруженные файлы

Проект полностью готов, включая HLS.js библиотеку:

```bash
# Все необходимые файлы уже включены в проект
# HLS.js библиотека уже находится в файле hls.js
```

### 2. Развертывание

- Загрузите все файлы на веб-сервер
- Убедитесь, что сервер поддерживает HTTPS (требуется для PWA и Service Worker)
- Откройте `index.html` в браузере

### 3. Локальная разработка

```bash
# Используйте любой локальный сервер, например:
python -m http.server 8000
# Или используйте Live Server в VS Code
```

## 🛠️ API функций

### Основные классы

#### App - главный класс приложения

```javascript
// Создание экземпляра приложения
const app = new App();
```

#### RadioPlayer - класс радио плеера

```javascript
// Запуск оптимизированного воспроизведения
startPlaybackOptimized();

// Остановка воспроизведения
stopPlayback();

// Переключение воспроизведения
togglePlayback();

// Управление звуком
toggleMute();
setVolume(volume); // 0 до 1
```

#### VideoPlayer - класс видео плеера

```javascript
// Открытие видео контента
openVideoContainer();

// Закрытие видео и освобождение ресурсов
closeVideo();
```

### Функции для отладки (в консоли браузера)

```javascript
// Получить логи
window.getRadioLogs();

// Скачать логи в JSON
window.downloadRadioLogs();

// Очистить логи
window.clearRadioLogs();

// Показать логи в таблице
window.showRadioLogs();

// Проверить статус звука на iOS
window.checkVolumeStatus();

// Включить кнопку отладки
window.enableDebugButton();
```

## 🐛 Отладка

### Просмотр логов

1. **В консоли браузера:**
   - Нажмите F12 для открытия DevTools
   - Используйте функции отладки указанные выше

2. **На отдельной странице:**
   - Откройте `logs.html` для интерактивного просмотра логов

3. **Через кнопку отладки:**
   - В консоли: `window.enableDebugButton()`
   - Нажмите кнопку поиска в нижнем правом углу

### Включение/отключение типов логов

```javascript
// В консоли браузера:
window.DEBUG_MODE = false; // отключить DEBUG логи
window.INFO_MODE = false; // отключить INFO логи
window.WARN_MODE = false; // отключить WARN логи
window.ERROR_MODE = false; // отключить ERROR логи
```

## 🔧 Конфигурация

### HLS.js параметры (в RadioPlayer.js)

```javascript
const hlsConfig = {
  lowLatencyMode: true, // Низкая задержка
  maxBufferLength: 3, // Макс буфер (сек)
  liveSyncDurationCount: 1, // Синхронизация
  fragLoadingTimeOut: 8000, // Таймаут загрузки фрагмента
  fragLoadingMaxRetry: 3, // Макс попыток
  // ...остальные параметры
};
```

### Поддерживаемые платформы

| Платформа                 | Статус            | Примечание                |
| ------------------------- | ----------------- | ------------------------- |
| iOS 13+                   | ✅ Нативный плеер | Лучшая производительность |
| iOS < 13                  | ✅ HLS.js         | Поддержка старых iPhone   |
| Android                   | ✅ HLS.js         | Полная поддержка          |
| Desktop (Chrome, Firefox) | ✅ HLS.js         | Полная поддержка          |
| Safari (macOS/iOS)        | ✅ Нативный       | Полная поддержка          |

## 🔐 Service Worker

Service Worker кэширует статические ресурсы:

- HTML, CSS, JavaScript
- Шрифты
- Иконки

**Исключения** (всегда запрашиваются из сети):

- HLS плейлисты (`.m3u8`)
- Сегменты потока (`.ts`, `.aac`)

Это обеспечивает работу приложения оффлайн при наличии кэша, но с актуальным аудиопотоком.

## 📄 Лицензия

MIT

## 🤝 Поддержка

Для отладки проблем используйте:

1. Функции отладки в консоли браузера
2. Страницу `logs.html`
3. DevTools (F12)

При необходимости можно получить экспортированные логи (функция `window.downloadRadioLogs()`).
